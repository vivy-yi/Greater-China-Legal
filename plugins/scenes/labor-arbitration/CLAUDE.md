<!--
Greater China Legal — Labor Arbitration Scene Configuration
=========================================================

本文件为 labor-arbitration 场景的运行时配置。
Skill执行时会读取此文件获取场景级上下文，用于定制化输出。

编辑规则：
- 法域足迹、数据源配置为必填项
- 公司定制信息（公司名称/业务描述）可按需填写
- 本文件不包含敏感信息（不含密码/API密钥）
-->

# Labor Arbitration Scene — Practice Profile

## Who's using this

**Role:** [律师 / 法务人员 / 业务部门（非法律背景，有律师支持）/ 业务部门（无律师支持）]
**Attorney contact:** [填空]

**工作成果头部标记：**
- 律师/法务人员 → `Privileged & Confidential — Attorney Work Product`
- 非法务（有律师支持）→ `Research Notes — Not Legal Advice — Review With Attorney Before Acting`
- 非法务（无律师支持）→ `General Information — Not Legal Advice — Consult A Licensed Attorney`

在产出工作成果前，必须先检查 Role 字段。如果 Role 为 `[填空]`，要求用户先设置角色。

---

*Written for: [公司名称] · 场景：劳动争议处理*
*Last updated: 2026-06*

---

## 公司基本信息

**公司名称：** [填空：公司名称]
**业务描述：** [填空：公司主营业务]
**员工规模：** [填空：员工数量]
**HR负责人：** [填空：HR负责人姓名/联系方式]
**外部律师：** [填空：劳动法专项律师姓名/律所/联系方式]

*这些信息用于在输出文档中自动填充抬头和升级路径。*

---

## 法域足迹

### 覆盖法域

| 法域 | 代码 | 说明 |
|---|---|---|
| 中国大陆 | cn-mainland | 主要运营地 |
| 香港 | hk | 子公司或分支机构 |
| 台湾 | tw | 供应商或代表处 |
| 澳门 | mo | 偶发性业务 |
| 新加坡 | sg | 东南亚区域中心 |

### 主要裁判地

**主要：** [填空：如"上海"或"全国通用"]
**次要：** [填空：如"北京"/"深圳"/"香港"等]

*Skill会根据员工实际工作地点选择对应法域的判断逻辑。*

---

## 数据源配置

### 实时数据源（按优先级）

| 优先级 | 数据源 | 用途 | 状态 |
|---|---|---|---|
| 1 | yuandian MCP（yuandian） | 法规/判例/案例检索 | [配置中/已就绪/不可用] |
| 2 | pkulaw MCP（pkulaw） | 北大法宝法律数据库 | [配置中/已就绪/不可用] |
| 3 | CLI工具（curl/命令行） | 备用查询 | [已就绪/不可用] |

### 数据源说明

- **yuandian MCP**：`yuandian-law-semantic` — 语义检索最准，适合查法规和类案
- **pkulaw MCP**：`pkulaw-law-semantic` — 裁判文书覆盖广，适合查仲裁裁决
- **CLI备用**：`curl` 调用 open.chineselaw.com API 或直接web搜索

### 不可用时的降级规则

| 数据源 | 不可用时的降级处理 |
|---|---|
| yuandian MCP | 改用 web_search 搜索"site:cac.gov.cn [关键词]" 或 "site:court.gov.cn [关键词]" |
| pkulaw MCP | 改用 yuandian 或 web_search |
| 两者均不可用 | 明确告知用户"无法实时查询，以一般规则输出，建议人工核实" |

---

## 管辖区识别规则

### 判断逻辑（按优先级）

```
第一步：员工实际工作地点
  → 读取员工劳动合同或HR系统中的工作地点字段
  → 适用该省市的裁判口径

第二步：公司注册地
  → 如劳动合同履行地不明确，适用公司注册地仲裁委管辖

第三步：约定管辖
  → 劳动仲裁不接受约定管辖（仲裁法规定）
  → 约定法院管辖在诉讼阶段有效，仲裁阶段无效
```

### 无数据时的处理

| 情形 | 处理方式 |
|---|---|
| 目标省市有数据 | 直接使用，引用来源 |
| 目标省市无数据，但同省有数据 | 使用同省数据，注明"参考[省份]口径" |
| 目标省市和省均无数据 | 使用全国一般标准，注明"无本地数据" |
| 多源数据矛盾 | 优先官方文件 > 法院判例 > 律师分析，标注不确定性 |

---

## 置信度评估

### 置信度等级

| 等级 | 条件 | 输出标注 |
|---|---|---|
| 高 | 多源一致（判例+文件+律师分析） | ✅ 可直接使用 |
| 中 | 单源权威（官方文件或高院判例） | ⚠️ 建议电话确认 |
| 低 | 多源矛盾或数据较旧（>3年） | 🔴 保守处理，呈现差异 |
| 无 | 完全查不到本地数据 | ⚠️ 使用全国一般标准 |

---

## 输出格式规范

### 工作产品头部

*以下头部自动附加在所有输出文档顶部：*

```
═══════════════════════════════════════
劳动争议分析备忘录
═══════════════════════════════════════
公司：[从上方"公司名称"字段填充]
日期：[自动填写]
案件编号：[如有Matter系统则填，无则留空]
═══════════════════════════════════════
```

### 保密标记

```
内部使用：CONFIDENTIAL — 内部法律分析
如须外部律师审核：附"PREPARED AT THE DIRECTION OF COUNSEL"
如用户为非律师：附"NOT LEGAL ADVICE — REVIEW WITH LICENSED ATTORNEY"
```

### 风险等级标注

| 风险等级 | 条件 | 标注 |
|---|---|---|
| 高 | 违法解除风险/2N赔偿/集体劳动争议 | 🔴 HIGH RISK — 须外部律师审核 |
| 中 | 标准补偿N/部分争议点不明确 | ⚠️ MEDIUM RISK — 建议确认 |
| 低 | 协商解除/无争议点 | ✅ LOW RISK — 可直接处理 |

---

## 升级矩阵

### 须升级至外部律师的情形

| 情形 | 升级门槛 | 升级路径 |
|---|---|---|
| 单个员工补偿金额 | 超过[填空：如"12个月工资"] | → 外部劳动法律师审核 |
| 集体劳动争议 | 涉及[填空：如"3人及以上"]同时解除 | → 外部律师 + 法总双重审核 |
| 特殊群体 | 孕期/工伤/医疗期内员工 | → 强制外部律师审核 |
| 敏感行业 | 互联网/金融/外资大厂 | → 按公司风险偏好决定 |

---

## 地区差异配置

### 已覆盖省市

| 省市 | 覆盖状态 | 说明 |
|---|---|---|
| 北京 | ✅ 已覆盖 | 仲裁委口径完整 |
| 上海 | ✅ 已覆盖 | 仲裁委口径完整 |
| 广东（深圳/广州） | ✅ 已覆盖 | 高院解答完整 |
| 浙江（杭州/宁波） | ✅ 已覆盖 | 仲裁委口径完整 |
| 江苏（南京/苏州） | ✅ 已覆盖 | 高院解答完整 |
| 四川（成都） | ✅ 已覆盖 | 仲裁委口径完整 |
| 重庆 | ✅ 已覆盖 | 仲裁委口径完整 |

### 未覆盖省市处理

如员工所在地不在上述列表中：
1. 使用同省数据（如有）
2. 如同省也无，使用全国一般标准
3. 结论标注"⚠️ 无本地数据，建议向当地12333热线确认"

---

## 常用查询入口

### 实时查询（如MCP已配置）

```
yuandian MCP：yuandian-law-semantic
  搜索格式："[城市] + [争议类型] + [具体问题]"

pkulaw MCP：pkulaw-law-semantic
  搜索格式："劳动争议 + [城市] + [案由]"
```

### Web备用查询

```
裁判文书网：https://wenshu.court.gov.cn/
  检索：案由 + 法院级别 + 裁判年份

国家人社部：https://www.mohrss.gov.cn/
  检索：劳动关系 + [城市] + 典型案例

各地人社局官网（见数据源清单references/数据源清单.md）
```


## 推理原子能力
## 推理原子能力调用流程

本场景的工作流程中，按以下顺序调用 `legal-atomic` 原子能力：

| 顺序 | 原子 Skill | 调用时机 |
|------|-----------|---------|
| 0 | `legal-element-extraction` | 收到用户输入后立即调用，将非结构化叙述转化为结构化法律事实 |
| 1 | `legal-norm-validity-check` | 在任何法条引用前调用，验证法条是否现行有效 |
| 2 | `deductive-reasoning` | 在分析阶段，将待判断的问题转化为 P-F-C 三段论格式 |
| 3 | `conflict-resolution` | 发现多个法条或请求权可能竞合时调用 |
| 4 | `evidence-argument-chain` | 需要组织证据与主张对应关系时调用 |
| 5 | `argument-strength-evaluation` | 输出结论前，标注论证强度（强/中/弱/存疑） |
| 6 | `legal-risk-assessment` | 在风险分级判断时调用 |
| 7 | `case-retrieval` | 需要检索类案时调用 |

### 追问规则（关键）

legal-element-extraction 的输出包含 `## 待补充事实` 节。如果该节非空：

1. **暂停当前分析流程**
2. 向用户逐一提问待补充事实，例如：
   > "请问合同中关于[知识产权归属/数据存储位置/价格调整机制]的条款是什么？这会影响后续判断。"
3. 用户补充后，**回到 Step 0 重新执行 legal-element-extraction**，将新信息并入结构化事实
4. 当待补充事实清空后，继续后续分析

**不得在待补充事实未清空的情况下输出最终结论。** 缺失关键事实的结论标注为「推定结论，须在事实补全后复核」。

| 顺序 | 原子 Skill | 调用时机 |
|------|-----------|---------|
| 0 | `legal-element-extraction` | 收到用户输入后立即调用，将非结构化叙述转化为结构化法律事实 |
| 1 | `legal-norm-validity-check` | 在任何法条引用前调用，验证法条是否现行有效 |
| 2 | `deductive-reasoning` | 在分析阶段，将待判断的问题转化为 P-F-C 三段论格式 |
| 3 | `conflict-resolution` | 发现多个法条或请求权可能竞合时调用 |
| 4 | `evidence-argument-chain` | 需要组织证据与主张对应关系时调用 |
| 5 | `argument-strength-evaluation` | 输出结论前，标注论证强度（强/中/弱/存疑） |
| 6 | `legal-risk-assessment` | 在风险分级判断时调用 |
| 7 | `case-retrieval` | 需要检索类案时调用 |

每个 scene skill 的工作流程第一步应为「法律要素提取」，最后一步前应为「论证强度评估」。


本场景在执行 legal analysis 时，按需调用以下 `legal-atomic` 原子 skill：

| 原子 Skill | 用途 | 调用时机 |
|-----------|------|---------|
| `legal-element-extraction` | 法律要素提取 | 所有输入预处理——将非结构化叙述转化为法律事实 |
| `legal-norm-validity-check` | 法条效力核查 | 引用法条前验证是否现行有效 |
| `deductive-reasoning` | P-F-C三段论推理 | 构建法律推理链时 |
| `conflict-resolution` | 法条竞合/冲突解决 | 多个法条或请求权竞合时 |
| `evidence-argument-chain` | 证据论证链 | 组织证据与主张对应关系时 |
| `argument-strength-evaluation` | 论证强度评估 | 输出结论时标注强/中/弱/存疑 |
| `legal-risk-assessment` | 法律风险评估 | 涉及风险分级判断时 |
| `case-retrieval` | 类案检索方法论 | 需要检索类案时 |

---

*Greater China Legal — B-phase labor-arbitration scene configuration*